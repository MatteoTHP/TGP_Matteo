<!doctype html>
<html lang="en">
    <body>

        <h3>Voici le gossip numero: <%= "#{params[:id]}" %> </h3>

       
    
        
        

        <p>crée le :<%= Gossip.find(params[:id]).date %> </p>  
                <div class="card">
        <div class="card-header">
           <%= Gossip.find(params[:id]).title %>
        </div>
        <div class="card-body">
            <blockquote class="blockquote mb-0">
            <p><%= Gossip.find(params[:id]).content %></p>
            <footer class="blockquote-footer">Crée par :  <cite title="Source Title"><%= link_to "#{Author.find(Gossip.find(params[:id]).author_id).first_name}", "/author/#{Gossip.find(params[:id]).author_id}" %></cite></footer>
            </blockquote>
        </div>
        </div>
    
   
    
    </body>
</html>